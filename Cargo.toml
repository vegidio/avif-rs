[package]
name = "avif-rust"
version = "0.1.0"
edition = "2024"
links = "avif"
build = "build.rs"

[dependencies]

[build-dependencies]
ureq = "3"
zip = { version = "2", default-features = false, features = ["deflate"] }
bindgen = "0.71"
